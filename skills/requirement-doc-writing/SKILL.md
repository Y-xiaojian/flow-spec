---

## name: requirement-doc-writing

description: 在需求已澄清、有业务场景或设计讨论结果后，编写 SRM 八章结构需求文档时使用
validation_status: production

# 需求文档编写

## 输入 → 输出 contract


| 输入     | 来源                                           | 是否必须             |
| ------ | -------------------------------------------- | ---------------- |
| 功能点清单  | `temp/requirements/<需求名>-feature-list.csv`（主）+ 同路径 `-feature-list.md`（元信息与统计） | ✅ write-doc 路线必须 |
| 头脑风暴结论 | `temp/brainstorm/<需求名>.md`                   | ✅                |
| 业务场景参考 | `references/templates/business-scenarios.md` | 建议读              |



| 输出        | 路径                                       |
| --------- | ---------------------------------------- |
| 需求文档（PRD） | `temp/requirements/<需求名>-prd.md`         |
| 业务流程图     | `temp/diagrams/<需求名>-process.drawio`（可选） |


若无功能点清单（非 write-doc 路线），至少需要「需求要点或设计讨论结果」作为替代输入。

---

## Overview

按规范产出 SRM 需求文档，采用 8 章结构（项目概述、需求范围、业务流程、功能需求详述、适配说明、非功能需求、风险与依赖、附录），供开发与测试使用。本 skill 由用户按需调用。

## 知识库（执行前必读）

执行本技能时，请优先查阅以下文件以获取规范与术语：

- `references/templates/requirement-spec-template.md` — **八章骨架 + 第四章固定结构（模块→业务介绍→功能点→概述/系统功能/页面）**
- `references/templates/srm-glossary.md` — 供应商、采购、合同等术语定义
- `references/templates/business-scenarios.md` — 典型业务场景参考
- **第二章泳道图**：绘制规约不在本文件，须遵循 `skills/swimlane-diagram/SKILL.md`（或加载 `flow-spec:swimlane-diagram`）

## 执行步骤（含 CHECKPOINT）

1. **确认输入** — 读取 **`feature-list.csv`**（及辅组 `.md` 若存在）、头脑风暴结论，确认输入完整；功能表以 **CSV** 行为准展开 FT 编号

⛔ **CHECKPOINT 1：输入确认** — 若输入不完整，停下列出缺失项，等用户补充

1. **文档信息** — 填写文档信息表、修订记录表
2. **一、项目概述** — 背景描述（含痛点表）、项目目标（按模式分目标表）、目标用户表
3. **二、需求范围** — 功能清单表（FT-xxx、功能模块、功能点、适用模式、优先级、状态）、**业务流程图（泳道）**及流程说明表；泳道 draw.io 的生成与样式遵循 `**swimlane-diagram`** 技能，产出 `temp/diagrams/<需求名>-process.drawio`，PRD 内写路径并插图（截图）
4. **三、业务流程** — 使用 **Mermaid** 编写协作类流程图（**时序图 `sequenceDiagram`、分支/路径 `flowchart` 等**），辅以流程说明表；**不要求、不编写单据状态机图**

⛔ **CHECKPOINT 2：大纲确认** — 输出一章到三章的大纲摘要，等用户确认结构无误再继续

1. **四、功能需求详述** — **严格采用** `requirement-spec-template.md` 中的层级：
  - `### 4.x 模块名` → `#### 业务介绍`（模块级：定位、痛点溯源、角色、边界）
  - `#### 4.x.y 功能点名（FT-…）` → `##### 概述`（目的、用户故事、典型步骤）→ `##### 系统功能`（前/后置、BR/VR、异常、通知、权限、验收、数据接口；**关键字段与校验可在此用表列出**）→ `##### 页面`（**仅一段说明**：如何打开 `temp/prototypes/<需求名>/index.html`、导航到哪个界面、建议截取哪些区域，并请**用户自行截图粘贴到 PRD 本节下方**；可附 `![附图占位](由用户粘贴)` 或空占位，**不再要求**在 PRD 内用完整字段表替代原型）
  - 禁止省略 **系统功能** 或 **业务介绍**；禁止仅「概述 + 一段页面说明」而 **系统功能** 无 BR/验收/异常。
2. **五、XXX 适配**（可选）— 下游系统或业务适配说明
3. **六、非功能需求** — 性能要求表、数据要求表
4. **七、风险与依赖** — 风险项表、外部依赖表
5. **八、附录** — 名词解释表、相关文档表
6. **保存（artifact）** — 保存到 `temp/requirements/<需求名>-prd.md`

⛔ **CHECKPOINT 3：最终确认** — 输出文档摘要（功能点数、章节列表），等用户确认归档

## 示例

- **主示例**：`examples/供应商自主条码打印与回传功能需求文档.md` — 完整 PRD 结构（8 章、功能清单表、Mermaid 流程图、流程说明、校验规则、风险依赖、附录）
- **流程图示例**：`docs/requirements/领料出库与物料流转追溯需求文档.md` — 时序图（sequenceDiagram）等（第三章不包含状态机时可只参考时序部分）
- **补充**：`examples/采购申请审批需求文档.md` — 简化版参考

## 第二章泳道图（委托 `swimlane-diagram`）

PRD **2.2 业务流程图**（跨职能泳道、draw.io XML）的**全部绘制规约**见 `**skills/swimlane-diagram/SKILL.md`**。编写第二章时请先加载 `**flow-spec:swimlane-diagram`**（或打开该 SKILL），再生成 `temp/diagrams/<需求名>-process.drawio`；本技能只要求在 PRD 中写明路径、脚注及**流程说明表**，并插入从 draw.io 导出的**截图**。

## 流程图（Mermaid，第三章节）

第三章使用 **Mermaid** 描述协作与分支逻辑，**不包含「单据状态机」类图**（状态转换用文字或流程说明表表达即可）。


| 场景      | 推荐类型                  | 说明      |
| ------- | --------------------- | ------- |
| 多方协作流程  | `sequenceDiagram`     | 角色间交互时序 |
| 分支/路径选择 | `flowchart` 或 `graph` | 多分支业务路径 |


**参考示例**：`docs/requirements/领料出库与物料流转追溯需求文档.md` 第 3.1 节（时序图部分）。

### 时序图绘制风格

- **参与者**：使用 `participant A as 显示名`，显示名简洁可读（如「领料申请人」「部门A/李四」）
- **分组**：使用 `rect rgb(r,g,b)` 将相关步骤分组，低饱和度背景区分阶段（如 `rgb(240,248,255)`、`rgb(255,250,240)`）
- **说明块**：用 `Note over 参与者: 说明` 标注阶段标题
- **消息**：`->>` 表示请求，`-->>` 表示返回；消息文案简洁、可含关键参数

## 写作要求

- 使用需求编号 **FT-xxx** 标识功能，数据校验规则 **VR-xxx**、业务规则 **BR-xxx**
- 多用表格呈现：痛点、目标、角色、功能清单、**系统功能中的规则与关键字段**、风险、依赖
- 二、需求范围：泳道图按 `**swimlane-diagram`** 技能输出 `.drawio`，PRD 内截图插图
- 三、业务流程：Mermaid 含**时序图与（如需）分支流程图**，**不含状态机图**；可附在线链接作补充
- 存在多种适用模式时，用对比表说明差异
- 功能详述须满足模板第四章：**模块下必有「业务介绍」；每个功能点必有「概述 / 系统功能 / 页面」**；系统功能中含 BR/VR、异常、验收及关键字段；**「页面」节为截图指引 + 用户贴图，不要求用完整字段表替代原型**
- 验收标准须可被测试执行（具体断言，避免「流畅」「友好」等不可验表述）

## 注意事项

- 使用 SRM 术语表统一用词
- 业务规则需明确、可执行
- 验收条件融入功能需求与流程说明，需可测试