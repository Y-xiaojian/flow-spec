---
description: fsx-route-delivery — 主编排（路线 A～D）
disable-model-invocation: true
---

请调用 `flow-spec:flow-spec-routing` 技能，并严格按加载后的全文执行。

**路径映射（storage.md）：** SKILL 中的 `temp/<类别>/` → **`temp/changes/<CHANGE-ID>/<类别>/`**；**CHANGE-ID** 读 **`temp/.active-change`**（默认 **CHG-local**）。定稿入 **`temp/specs/<类别>/`**。
