---
description: fsx-write-prd — PRD（八章结构）
disable-model-invocation: true
---

请按以下步骤执行：

1. 阅读 `skills/using-flow-spec/SKILL.md`
2. 调用 `flow-spec:requirement-doc-writing` 技能，并严格按加载后的全文执行。

**路径映射（storage.md）：** SKILL 中的 `temp/<类别>/` → **`temp/changes/<CHANGE-ID>/<类别>/`**；**CHANGE-ID** 读 **`temp/.active-change`**（默认 **CHG-local**）。定稿入 **`temp/specs/<类别>/`**。