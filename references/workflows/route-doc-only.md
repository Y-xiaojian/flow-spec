# Route C 单文档修改

1. 头脑风暴（输出 `changes/<CHANGE-ID>/brainstorm/`）
2. 任务计划（输出 `changes/<CHANGE-ID>/tasks/`）
3. 定位目标文档与版本
4. 填写变更说明（原因、影响范围）
5. 修订文档正文
6. 执行一致性检查（术语、编号、引用）
7. 输出到 `changes/<CHANGE-ID>/…` 及 `logs/` 并记录日志（见 `storage.md`）
