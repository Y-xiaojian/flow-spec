# Route A 新增文档包（主路线摘要）

1. 需求风暴（`requirement-brainstorming`，输出 `temp/brainstorm/`）
2. 任务计划（输出 `temp/tasks/`，模板见 `references/templates/task-plan.md`）
3. 原型（`prototype-design`，输出 `temp/prototypes/`）
4. 功能点清单（`feature-list`，输出 `temp/requirements/`）
5. 需求文档（`requirement-doc-writing`，输出 `temp/requirements/`）
6. 按需泳道图（`swimlane-diagram`，输出 `temp/diagrams/`）
7. 技术说明文档（可选，`technical-doc-writing`，输出 `temp/technical/`）

输出顺序与目录约定见 `references/rules/storage.md`。
